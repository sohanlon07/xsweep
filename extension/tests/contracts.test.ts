import { describe,expect,it } from 'vitest'; import { isMessage } from '../src/shared/messages';
describe('message validation',()=>{it('rejects malformed destructive work',()=>{expect(isMessage({type:'mutate',targetId:'x'})).toBe(false);expect(isMessage({type:'mutate',runId:'r',attemptId:'a',itemId:'i',action:'delete_post',targetId:'1',accountId:'42',accountHandle:'alice',transport:'direct'})).toBe(true);expect(isMessage({type:'mutate',runId:'r',attemptId:'a',itemId:'i',action:'delete_post',targetId:'1',transport:'direct'})).toBe(false);expect(isMessage({type:'probe',transport:'unknown'})).toBe(false);expect(isMessage({type:'direct-discover',runId:'r',category:'likes',handle:'alice'})).toBe(true);});});
