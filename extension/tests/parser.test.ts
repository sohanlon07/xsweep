import { describe, expect, it } from 'vitest'; import { parseEntry, unwrapArchiveAssignment } from '../src/archive/parser';
describe('archive parser',()=>{it('only accepts known assignments',()=>expect(()=>unwrapArchiveAssignment('alert(1)')).toThrow());it('supports multipart tweet data and decimal ids',()=>{const r=parseEntry('data/tweets-part1.js','window.YTD.tweets.part1 = [{"tweet":{"id_str":"123","full_text":"hello","created_at":"2025-01-02T00:00:00Z"}}];');expect(r[0]).toMatchObject({targetId:'123',action:'delete_post',kind:'tweet'});});it('categorises replies from archive metadata',()=>{const r=parseEntry('data/tweets.js','[{"tweet":{"id_str":"124","full_text":"reply","in_reply_to_status_id_str":"99"}}]');expect(r[0]).toMatchObject({targetId:'124',kind:'reply'});});it('deduces likes without inventing an action date',()=>{const r=parseEntry('data/like.js','[{"like":{"tweetId":"456","fullText":"x"}}]');expect(r[0]).toMatchObject({action:'unlike',kind:'like',occurredAt:undefined});});});
